export namespace RegisterCandidate {
  export type Params = {
    name: string;
    email: string;
    password: string;
    phone: string;
  };
  export type Model = {
    name: string;
    email: string;
    password: string;
    phone: string;
  };
}
